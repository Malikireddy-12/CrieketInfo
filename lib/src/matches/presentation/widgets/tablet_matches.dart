import 'package:flutter/material.dart';

class TabletMatches extends StatelessWidget {
  const TabletMatches({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}