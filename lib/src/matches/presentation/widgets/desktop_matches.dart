import 'package:flutter/material.dart';

class DesktopMatches extends StatelessWidget {
  const DesktopMatches({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}